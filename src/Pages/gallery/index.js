import HeaderSection from "@aio/components/HeaderSection";

const Profile = (props) => {
    return (
        <>
            <HeaderSection 
                heading={'This is the Gallery Section'}
                subHeading={'Lets check your stats today!'}
            />
        </>
    );
}

export default Profile;